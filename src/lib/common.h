#pragma once
#include "appSingleton.h"

namespace Window {
	const int left  = -WindowSize::WIDTH  * 0.5;
	const int right =  WindowSize::WIDTH  * 0.5;
	const int up    =  WindowSize::HEIGHT * 0.5;
	const int down  = -WindowSize::HEIGHT * 0.5;
}